package run;

import container.Strategy;
import runner.StrategyTaskRunner;

public class Main {
    public static void main(String[] args) {
        //TestRunner.run(args[0]);
        TestSorting.run(args[0]);
    }
}